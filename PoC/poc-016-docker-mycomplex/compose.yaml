services:
  mycomplex-poc:
    build:
      context: .
      dockerfile: Dockerfile
    image: mycomplex/poc-016-docker:local
    container_name: mycomplex-poc-016
    ports:
      - "8087:8080"
    environment:
      SERVER_PORT: 8080
      APP_ENVIRONMENT: docker-compose
    restart: unless-stopped
